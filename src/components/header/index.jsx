import styled  from 'styled-components';

const StyledHeader = styled.header`
    background-color: #000000;
`;

export const Header = () => {
    return (
        <StyledHeader>
            <h1>Header</h1>
        </StyledHeader>
    )
}
    
